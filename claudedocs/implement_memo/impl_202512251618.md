# Implementation Memo – 2025-12-25 16:18

本文件逐步紀錄依核對單所進行的每一項實作動作（只記錄事實與結果）。

## Step 1 – 更新 docker-compose.yml backend OLLAMA_MODEL 為 $

- 位置：docker-compose.yml → services.backend.environment
- 動作：將 `- OLLAMA_MODEL=gpt-oss:20b` 改為 `- OLLAMA_MODEL=${OLLAMA_MODEL}`。
- 目的：允許以 `.env` 或啟動參數注入模型名稱。
- 結果：完成。

## Step 2 – 更新 docker-compose.yml presenton OLLAMA_MODEL 為 $

- 位置：docker-compose.yml → services.presenton.environment
- 動作：將多重 OLLAMA_MODEL 固定值刪除，改為單一 `- OLLAMA_MODEL=${OLLAMA_MODEL}`。
- 目的：與 backend 一致，集中由環境變數控制模型選擇。
- 結果：完成。

（後續步驟完成後續更新。）

## Step 3 – 將 zephyr_service.py 的模型改為 phi4-mini-reasoning:3.8b

- 位置：backend/app/services/zephyr_service.py
- 動作：`self.model` 改為 `phi4-mini-reasoning:3.8b`，並更新類別註解敘述。
- 目的：依需求將 Transcript 生成使用之模型切換為 phi4-mini-reasoning。
- 結果：完成。

## Step 4 – 調整 zephyr_service.py 的模型可用性檢查邏輯

- 位置：backend/app/services/zephyr_service.py → check_model_availability()
- 動作：偵測條件從包含 `zephyr` 改為包含 `phi4-mini-reasoning:3.8b` 或（寬鬆）`phi4-mini`；下載邏輯亦改為拉取 `phi4-mini-reasoning:3.8b`。
- 目的：確保健康檢查與自動下載符合新模型。
- 結果：完成。

## Step 5 – 在 routes.py 新增 transcripts_cache 避免 NameError

- 位置：backend/app/api/routes.py
- 動作：在模組頂部新增 `transcripts_cache = {}`。
- 目的：避免 Transcript 相關端點使用未定義變數導致 NameError。
- 結果：完成。

## Step 6 – 更新 routes.py transcript 503 提示為 phi4-mini-reasoning:3.8b

- 位置：backend/app/api/routes.py → /api/transcript/generate 端點
- 動作：將錯誤訊息由「ollama pull zephyr:7b」改為「ollama pull phi4-mini-reasoning:3.8b」。
- 目的：對應新模型的指引提示。
- 結果：完成。

## Step 7 – 更新 frontend/index.html 的 Transcript 錯誤提示與判斷

- 位置：frontend/index.html → 產生演講稿錯誤處理區塊
- 動作：錯誤訊息判斷改為檢查 `phi4-mini`；提示拉取 `phi4-mini-reasoning:3.8b`。
- 目的：前端提示與後端行為一致。
- 結果：完成。

## Step 8 – 將 scripts/start_system.sh 改用 docker compose 指令

- 位置：scripts/start_system.sh
- 動作：所有 `docker-compose` 改為 `docker compose`；Compose 版本檢查改為 v2。
- 目的：與你的環境（2.39.2）一致。
- 結果：完成。

## Step 9 – 將 scripts/stop_system.sh 改用 docker compose 指令

- 位置：scripts/stop_system.sh
- 動作：`docker-compose ps/down` 改為 `docker compose ps/down`。
- 目的：統一使用 Compose v2 指令。
- 結果：完成。

## Step 10 – start_system.sh 將 zephyr 檢查改為 phi4-mini-reasoning:3.8b

- 位置：scripts/start_system.sh → 模型檢查段
- 動作：由檢查/下載 `zephyr:7b` 改為 `phi4-mini-reasoning:3.8b`。
- 目的：與 Transcript 模型切換一致。
- 結果：完成。

## Step 11 – 新增 docker-compose.override.yml 覆蓋 presenton 映像與平台

- 位置：docker-compose.override.yml（專案根目錄）
- 動作：新增檔案，僅覆蓋 `services.presenton.image: presenton:arm64-local` 與 `services.presenton.platform: linux/arm64`。
- 目的：在 Apple Silicon（arm64）優先使用本機 arm64 版本 Presenton 映像。
- 結果：完成。
