# Implementation Memo – 2025-12-25 16:18

本文件逐步紀錄依核對單所進行的每一項實作動作（只記錄事實與結果）。

## Step 1 – 更新 docker-compose.yml backend OLLAMA_MODEL 為 $

- 位置：docker-compose.yml → services.backend.environment
- 動作：將 `- OLLAMA_MODEL=gpt-oss:20b` 改為 `- OLLAMA_MODEL=${OLLAMA_MODEL}`。
- 目的：允許以 `.env` 或啟動參數注入模型名稱。
- 結果：完成。

## Step 2 – 更新 docker-compose.yml presenton OLLAMA_MODEL 為 $

- 位置：docker-compose.yml → services.presenton.environment
- 動作：將多重 OLLAMA_MODEL 固定值刪除，改為單一 `- OLLAMA_MODEL=${OLLAMA_MODEL}`。
- 目的：與 backend 一致，集中由環境變數控制模型選擇。
- 結果：完成。

（後續步驟完成後續更新。）

## Step 3 – 將 zephyr_service.py 的模型改為 phi4-mini-reasoning:3.8b

- 位置：backend/app/services/zephyr_service.py
- 動作：`self.model` 改為 `phi4-mini-reasoning:3.8b`，並更新類別註解敘述。
- 目的：依需求將 Transcript 生成使用之模型切換為 phi4-mini-reasoning。
- 結果：完成。

## Step 4 – 調整 zephyr_service.py 的模型可用性檢查邏輯

- 位置：backend/app/services/zephyr_service.py → check_model_availability()
- 動作：偵測條件從包含 `zephyr` 改為包含 `phi4-mini-reasoning:3.8b` 或（寬鬆）`phi4-mini`；下載邏輯亦改為拉取 `phi4-mini-reasoning:3.8b`。
- 目的：確保健康檢查與自動下載符合新模型。
- 結果：完成。

## Step 5 – 在 routes.py 新增 transcripts_cache 避免 NameError

- 位置：backend/app/api/routes.py
- 動作：在模組頂部新增 `transcripts_cache = {}`。
- 目的：避免 Transcript 相關端點使用未定義變數導致 NameError。
- 結果：完成。

## Step 6 – 更新 routes.py transcript 503 提示為 phi4-mini-reasoning:3.8b

- 位置：backend/app/api/routes.py → /api/transcript/generate 端點
- 動作：將錯誤訊息由「ollama pull zephyr:7b」改為「ollama pull phi4-mini-reasoning:3.8b」。
- 目的：對應新模型的指引提示。
- 結果：完成。

## Step 7 – 更新 frontend/index.html 的 Transcript 錯誤提示與判斷

- 位置：frontend/index.html → 產生演講稿錯誤處理區塊
- 動作：錯誤訊息判斷改為檢查 `phi4-mini`；提示拉取 `phi4-mini-reasoning:3.8b`。
- 目的：前端提示與後端行為一致。
- 結果：完成。

## Step 8 – 將 scripts/start_system.sh 改用 docker compose 指令

- 位置：scripts/start_system.sh
- 動作：所有 `docker-compose` 改為 `docker compose`；Compose 版本檢查改為 v2。
- 目的：與你的環境（2.39.2）一致。
- 結果：完成。

## Step 9 – 將 scripts/stop_system.sh 改用 docker compose 指令

- 位置：scripts/stop_system.sh
- 動作：`docker-compose ps/down` 改為 `docker compose ps/down`。
- 目的：統一使用 Compose v2 指令。
- 結果：完成。

## Step 10 – start_system.sh 將 zephyr 檢查改為 phi4-mini-reasoning:3.8b

- 位置：scripts/start_system.sh → 模型檢查段
- 動作：由檢查/下載 `zephyr:7b` 改為 `phi4-mini-reasoning:3.8b`。
- 目的：與 Transcript 模型切換一致。
- 結果：完成。

## Step 11 – 新增 docker-compose.override.yml 覆蓋 presenton 映像與平台

- 位置：docker-compose.override.yml（專案根目錄）
- 動作：新增檔案，僅覆蓋 `services.presenton.image: presenton:arm64-local` 與 `services.presenton.platform: linux/arm64`。
- 目的：在 Apple Silicon（arm64）優先使用本機 arm64 版本 Presenton 映像。
- 結果：完成。

Note:

建議你在 TeacherAssist 專案根目錄驗證：* 檢視合併結果:

* **docker compose config | sed -n '/presenton:/,/^[^ ]/p'**
* 應可看到 **image: presenton:arm64-local** 與 **platform: linux/arm64**
* 啟動服務:
* **./scripts/start_system.sh**
* 確認映像來源:
* **docker compose images | grep presenton**（應顯示 presenton:arm64-local）
* 確認服務:
* **curl http://localhost:5050/api/health**
* 瀏覽 **http://localhost:8080**

若要改回遠端映像，刪除或暫時改名 **docker-compose.override.yml** 後再啟動即可。

----------------------------------------------------------------

This session is being continued from a previous conversation that ran out of context. The conversation is summarized below:
Analysis:
Let me chronologically analyze this conversation to capture all important details:

1. **Initial Request**: User ran `scripts/start_system.sh` and encountered a Docker port 8000 allocation error, despite no process occupying that port.
2. **Root Cause Investigation**:

   - Found `docker-compose.yml` had `ports: - "8000:8000"` for presenton
   - Found `docker-compose.override.yml` had `ports: - "8000:3000"` for presenton
   - This created a conflict where Docker tried to bind TWO container ports to the same host port 8000
3. **First Fix - Port Mapping**:

   - Removed `ports` section from docker-compose.yml
   - Updated `PRESENTON_API_URL` from `http://presenton:8000` to `http://presenton:3000`
   - Removed `OLLAMA_HOST` environment variable from .env files (was causing Pydantic validation errors)
4. **Second Issue - Architecture Support**:

   - User needed cross-architecture support (AMD64 at office, ARM64 at home)
   - Implemented dynamic architecture detection in `start_system.sh`
   - Modified `docker-compose.override.yml` to use environment variables `${PRESENTON_IMAGE}` and `${DOCKER_PLATFORM}`
5. **Third Issue - API 404 Error**:

   - User tried to generate PPT, got 404 on `/api/v1/ppt/presentation/generate`
   - Discovered port mapping was wrong: should be `8000:8000` not `8000:3000`
   - Presenton container runs Python API server on port 8000, not 3000
   - Fixed by changing back to `8000:8000` in docker-compose.override.yml
   - Updated backend URL back to `http://presenton:8000`
6. **Fourth Issue - Model Download**:

   - Presenton downloads 79MB `chroma/models/onnx.tar.gz` on every startup
   - This causes 5-10 minute delay before API is available
   - User requested persistent storage solution
7. **Current Work - Persistent Model Storage**:

   - Added volume mount: `./app_data/chroma:/app/servers/fastapi/chroma` to docker-compose.override.yml
   - Created `app_data/chroma/models` directory
   - Model download was in progress when restart happened
   - After restart, the directory shows empty - volume mount may have cleared in-progress download

Key errors encountered:

- Port allocation conflict (solved by removing duplicate port mappings)
- OLLAMA_HOST validation error (solved by removing unsupported env var)
- Wrong port mapping 8000:3000 (solved by changing to 8000:8000)
- Environment variable conflicts between Docker Desktop and OrbStack contexts

Summary:

## 1. Primary Request and Intent:

The user had multiple sequential requests:

1. **Initial**: Fix "port is already allocated" error on port 8000 when running `scripts/start_system.sh`, despite no process occupying the port
2. **Architecture Support**: Implement cross-architecture support for AMD64 (office) and ARM64 (home) machines
3. **API Error**: Fix 404 error when trying to generate PPT from frontend: `Client error '404 Not Found' for url 'http://presenton:3000/api/v1/ppt/presentation/generate'`
4. **Model Persistence**: Implement solution to download `chroma/models/onnx.tar.gz` only once and persist it locally, so subsequent startups don't re-download the 79MB model

## 2. Key Technical Concepts:

- Docker Compose port mapping conflicts and override files
- Docker multi-architecture support (linux/amd64 vs linux/arm64)
- Docker Buildx for cross-platform image building
- Environment variable substitution in docker-compose.yml
- Volume mounting for data persistence
- Pydantic Settings validation in FastAPI
- OrbStack vs Docker Desktop context management
- Container networking and internal vs external port mapping
- Presenton API architecture (Python FastAPI on port 8000, Next.js frontend on port 3000)
- ChromaDB model downloading and caching

## 3. Files and Code Sections:

### `/Users/xrickliao/WorkSpaces/Work/Projects/TeacherAssist/docker-compose.yml`

**Why important**: Main Docker Compose configuration file
**Changes made**:

1. Removed duplicate port mapping `- "8000:8000"` from presenton service
2. Updated `PRESENTON_API_URL` from `http://presenton:3000` to `http://presenton:8000` (then back again)

**Final state**:

```yaml
services:
  presenton:
    image: ghcr.io/presenton/presenton:latest
    container_name: presenton-api
    environment:
      - PRESENTON_API_KEY=sk-presenton-...
      - LLM=ollama
      - OLLAMA_URL=http://host.docker.internal:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL}
      - IMAGE_PROVIDER=pexels
      - PEXELS_API_KEY=...
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app-network
    restart: unless-stopped

  backend:
    build: ./backend
    container_name: ppt-backend
    ports:
      - "5050:5000"
    environment:
      - PRESENTON_API_URL=http://presenton:8000  # Updated back to 8000
      - PRESENTON_API_KEY=...
      - OLLAMA_URL=http://host.docker.internal:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL}
      - PEXELS_API_KEY=...
      - CORS_ORIGINS=*
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./backend:/app
      - ./output:/app/output
    depends_on:
      - presenton
    networks:
      - app-network
    restart: unless-stopped
```

### `/Users/xrickliao/WorkSpaces/Work/Projects/TeacherAssist/docker-compose.override.yml`

**Why important**: Override file for local customizations and architecture-specific settings
**Changes made**:

1. Changed from `8000:3000` to `8000:8000` port mapping
2. Added environment variable placeholders `${PRESENTON_IMAGE}` and `${DOCKER_PLATFORM}`
3. Added volume mount for persistent Chroma model storage

**Final state**:

```yaml
services:
  presenton:
    image: ${PRESENTON_IMAGE:-ghcr.io/presenton/presenton:latest}
    platform: ${DOCKER_PLATFORM:-linux/amd64}
    ports:
      - "8000:8000"
    volumes:
      - ./app_data/chroma:/app/servers/fastapi/chroma
```

### `/Users/xrickliao/WorkSpaces/Work/Projects/TeacherAssist/scripts/start_system.sh`

**Why important**: System startup script that needs to handle different architectures
**Changes made**: Added Step 0 for automatic architecture detection

**Added code (lines 29-63)**:

```bash
# ===== Step 0: 架構檢測與環境配置 =====
echo "Step 0: 架構檢測"
echo "---------------"

# 檢測系統架構
ARCH=$(uname -m)
case "$ARCH" in
    x86_64|amd64)
        export DOCKER_PLATFORM="linux/amd64"
        export PRESENTON_IMAGE="ghcr.io/presenton/presenton:latest"
        print_success "架構: AMD64 (x86_64)"
        print_info "將使用官方 Presenton 鏡像"
        ;;
    arm64|aarch64)
        export DOCKER_PLATFORM="linux/arm64"
        # 檢查本地是否有 ARM64 鏡像
        if docker images | grep -q "presenton.*arm64-local"; then
            export PRESENTON_IMAGE="presenton:arm64-local"
            print_success "架構: ARM64 (aarch64)"
            print_info "將使用本地構建的 ARM64 鏡像: presenton:arm64-local"
        else
            export PRESENTON_IMAGE="ghcr.io/presenton/presenton:latest"
            print_success "架構: ARM64 (aarch64)"
            print_warning "本地無 ARM64 鏡像,將嘗試使用官方鏡像（可能需要構建）"
            print_info "建議執行: docker buildx build --platform linux/arm64 -t presenton:arm64-local ."
        fi
        ;;
    *)
        print_error "不支援的架構: $ARCH"
        print_info "支援的架構: x86_64 (AMD64), arm64 (ARM64)"
        exit 1
        ;;
esac
```

### `/Users/xrickliao/WorkSpaces/Work/Projects/TeacherAssist/.env`

**Why important**: Environment configuration file
**Changes made**:

1. Removed `OLLAMA_HOST=http://localhost:11434` (line 8) - this was causing Pydantic validation errors
2. Added commented documentation for Docker platform configuration

**Added documentation**:

```bash
# Docker Platform Configuration (自動檢測，也可手動覆蓋)
# DOCKER_PLATFORM=linux/amd64  # 辦公室 (AMD64)
# DOCKER_PLATFORM=linux/arm64  # 家裡 (ARM64)
# PRESENTON_IMAGE=ghcr.io/presenton/presenton:latest  # AMD64 官方鏡像
# PRESENTON_IMAGE=presenton:arm64-local               # ARM64 本地鏡像
```

### `/Users/xrickliao/WorkSpaces/Work/Projects/TeacherAssist/backend/.env`

**Why important**: Backend-specific environment configuration
**Changes made**: Same as main .env - removed `OLLAMA_HOST` and added platform documentation

### `/Users/xrickliao/WorkSpaces/Work/Projects/TeacherAssist/app_data/chroma/`

**Why important**: Newly created directory for persistent Chroma model storage
**Changes made**: Created directory structure `app_data/chroma/models/`

## 4. Errors and Fixes:

### Error 1: Port 8000 Already Allocated

**Error**: `Bind for :::8000 failed: port is already allocated`
**Root Cause**: docker-compose.yml had `8000:8000` AND docker-compose.override.yml had `8000:3000`, causing Docker to try binding two container ports to the same host port 8000
**Fix**: Removed the `ports` section from docker-compose.yml, keeping only the override configuration
**User Feedback**: User confirmed they could run the system after this fix

### Error 2: Pydantic Validation Error

**Error**:

```
pydantic_core._pydantic_core.ValidationError: 1 validation error for Settings
ollama_host
  Extra inputs are not permitted [type=extra_forbidden, input_value='http://host.docker.internal:11434', input_type=str]
```

**Root Cause**: Both `.env` and `backend/.env` contained `OLLAMA_HOST` environment variable, which is not a valid field in the Pydantic Settings model (should be `OLLAMA_URL`)
**Fix**: Removed `OLLAMA_HOST` from both .env files
**User Feedback**: After fix, backend health check showed healthy status

### Error 3: 404 Not Found on API Endpoint

**Error**: `Client error '404 Not Found' for url 'http://presenton:3000/api/v1/ppt/presentation/generate'`
**Root Cause**: Port mapping was set to `8000:3000` but Presenton's Python API server actually runs on port 8000 (not 3000). Port 3000 is the Next.js frontend.
**Discovery Process**:

- Checked running processes in container: `python server.py --port 8000`
- Checked listening ports: Port 8000 had Python API, port 3000 had Next.js
  **Fix**: Changed port mapping back to `8000:8000` and updated backend URL back to `http://presenton:8000`
  **User Feedback**: User reported the API endpoint error which led to this discovery

### Error 4: Slow Startup Due to Model Download

**Error**: Presenton takes 5-10 minutes to start because it downloads 79MB `chroma/models/onnx.tar.gz` every time
**Root Cause**: Model stored in container filesystem, lost on restart
**Fix**: Added volume mount `./app_data/chroma:/app/servers/fastapi/chroma` to persist the model
**User Feedback**: User explicitly requested: "how to download the chroma/models/onnx.tar.gz only once, and save it to onnx folder, when we start system next time, we just load local"
**Status**: Fix implemented but needs verification after complete model download

## 5. Problem Solving:

**Solved Problems**:

1. ✅ Docker port allocation conflict resolved by removing duplicate port mappings
2. ✅ Cross-architecture support (AMD64/ARM64) implemented with automatic detection
3. ✅ Environment variable validation error fixed by removing unsupported OLLAMA_HOST
4. ✅ API 404 error fixed by correcting port mapping to 8000:8000
5. ✅ DOCKER_HOST environment variable conflict resolved by using `unset DOCKER_HOST`

**Ongoing Troubleshooting**:

1. **Model Persistence**: Volume mount added but container was restarted during model download (at 48MB/79MB progress). Need to verify that:
   - Model completes downloading
   - Model persists in `./app_data/chroma/models/`
   - Subsequent restarts use cached model instead of re-downloading

## 6. All User Messages:

1. "I run scripts/start_system.sh" and the error occured, but I tried several commands, there is no any pid occupy port 8000. error: [port already allocated error]
2. "選項 1：只保留 override 的配置"
3. "you are great, I can run the system. However I try to test generate ppt from a segment of text, the errors occured: [404 error on API]"
4. "our machine is arm64, so I use docker buildx to build a image for arm64, you can't and no need to pull a presenton image from network.now keep find why port 8000 is occuied"
5. "ok, use 方案 A：基於環境變數的動態配置"
6. "there is another issue: When I work on this project in office, may machine is amd64, and when I work on this project at home, my machine is arm64. 1. current start_system.sh and stop_system.sh can satisfy my situation?"
7. "/design how to download the chroma/models/onnx.tar.gz only once, and save it to onnx folder, when we start system next time, we just load local"

## 7. Pending Tasks:

1. **Verify Model Persistence**: Wait for Chroma model download to complete and verify that:

   - The full 79MB `onnx.tar.gz` is saved in `./app_data/chroma/models/`
   - Next container restart uses the cached model
   - API becomes available quickly without re-downloading
2. **Test Cross-Architecture**: Verify the system works correctly on AMD64 machines at the office (currently only tested on ARM64)
3. **Test PPT Generation**: Once model is fully loaded, test the PPT generation functionality that was failing with 404 errors

## 8. Current Work:

Immediately before the summary request, I was implementing persistent storage for the Chroma model (79MB `onnx.tar.gz`) to avoid re-downloading on every container restart.

**Specific actions taken**:

1. Discovered model location in container: `/app/servers/fastapi/chroma/models/onnx.tar.gz`
2. Modified `docker-compose.override.yml` to add volume mount:
   ```yaml
   volumes:
     - ./app_data/chroma:/app/servers/fastapi/chroma
   ```
3. Created host directory: `mkdir -p app_data/chroma/models`
4. Observed that model was downloading (48MB/79MB complete)
5. Restarted container with: `docker compose restart presenton`
6. After restart, noticed the `app_data/chroma/models/` directory was empty

**Issue discovered**: The container restart during model download may have interrupted the process. The volume mount needs to complete a full download cycle to verify persistence works correctly.

## 9. Optional Next Step:

**Next Step**: Monitor the model download completion and verify persistence functionality.

**Direct quote from conversation**: User requested "how to download the chroma/models/onnx.tar.gz only once, and save it to onnx folder, when we start system next time, we just load local"

**Current status**: Volume mount has been added but the model download was interrupted by container restart.

**Recommended action**:

1. Wait for the current model download to complete (monitor with `docker logs -f presenton-api | grep "chroma/models"`)
2. Verify the model file appears in `./app_data/chroma/models/onnx.tar.gz` on the host
3. Test by restarting the container and confirming it uses the cached model
4. Document the startup time improvement for the user.
   Please continue the conversation from where we left it off without asking the user any further questions. Continue with the last task that you were asked to work on.
