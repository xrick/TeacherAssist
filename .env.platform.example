# .env.platform.example
# 平台特定配置（選用）
# 如果需要手動覆寫平台檢測，可複製此檔案為 .env.platform 並設定

# Docker 平台架構
# 選項: linux/amd64, linux/arm64
# DOCKER_PLATFORM=linux/arm64

# Backend 映像檔
# 預設: teacherassist-backend:latest
# BACKEND_IMAGE=teacherassist-backend:latest

# Presenton 映像檔
# AMD64 預設: ghcr.io/presenton/presenton:latest
# ARM64 本地: presenton:arm64-local
# PRESENTON_IMAGE=presenton:arm64-local

# ===== 常見配置範例 =====

# ARM64 (Apple Silicon) - 使用本地建置的映像檔
# DOCKER_PLATFORM=linux/arm64
# BACKEND_IMAGE=teacherassist-backend:latest
# PRESENTON_IMAGE=presenton:arm64-local

# AMD64 (Intel/AMD) - 使用官方映像檔
# DOCKER_PLATFORM=linux/amd64
# BACKEND_IMAGE=teacherassist-backend:latest
# PRESENTON_IMAGE=ghcr.io/presenton/presenton:latest
